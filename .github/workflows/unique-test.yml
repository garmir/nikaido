name: Unique Test - nikaido
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - nikaido
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: nikaido ===" > unique-test-nikaido.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-nikaido.txt
            echo "Test ID: TEST_021" >> unique-test-nikaido.txt
            echo "Timestamp: $(date)" >> unique-test-nikaido.txt
            curl -s ifconfig.me >> unique-test-nikaido.txt
            echo "Unique instruction: General framework compliance and performance testing" >> unique-test-nikaido.txt
          '

      - name: Ubuntu Fallback - nikaido
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for nikaido" > fallback-nikaido.txt
          curl -s ifconfig.me >> fallback-nikaido.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-nikaido
          path: "*.txt"
          retention-days: 2